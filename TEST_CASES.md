# 이어봄 최신 테스트 케이스 및 체크리스트

> 마지막 갱신: 2026-08-03
>
> 대상 브랜치: `feature/ai-engine`
>
> 자동 테스트 명령: `node --test tests/*.test.mjs`

## 1. 최종 자동 테스트 결과

- [x] 전체 테스트 실행 완료
- [x] 총 52개 통과
- [x] 실패 0개
- [x] JavaScript 구문 검사 통과
- [x] `git diff --check` 기준 공백 오류 없음

자동 테스트 파일:

- `tests/ai-contract.test.mjs`
- `tests/demo-db.test.mjs`
- `tests/history-storage.test.mjs`
- `tests/impact-feedback.test.mjs`
- `tests/mentor-answer.test.mjs`
- `tests/solar-handler.test.mjs`
- `tests/stt.test.mjs`
- `tests/ui-state.test.mjs`

## 2. AI 고민 분석 및 경험 매칭

### AI-00. 모호한 고민 추가 질문

- [x] `needsClarification: true`이면 `route`를 `CLARIFICATION`으로 일관되게 정규화한다.
- [x] Solar가 `IN_SCOPE`로 반환해도 “출산이 걱정돼요”처럼 주제만 있는 최초 입력은 JavaScript가 `CLARIFICATION`으로 보정하고 해당 주제의 구체적인 상황을 묻는다.
- [x] “그냥 너무 힘들어요”처럼 원인을 알 수 없는 입력에는 최근의 실제 상황을 하나 묻는다.
- [x] “입덧이 심해요”처럼 짧아도 구체적인 상황이 확인되면 글자 수만으로 추가 질문을 강제하지 않는다.
- [x] 추가 답변은 원래 고민과 `clarificationHistory`로 함께 분석한다.
- [x] 추가 답변으로 구체적인 상황이 확인되면 `IN_SCOPE`로 진행한다.
- [x] 화면은 `route === "CLARIFICATION"` 또는 `needsClarification === true`이면 추가 질문 영역을 표시한다.
- [x] 저장된 추가 질문과 기존 이력이 없는 구형 세션도 안전하게 처리한다.
- [ ] Solar 실패 fallback에서 두 번째 이후의 모호한 추가 답변까지 재질문하는 동작은 별도 보강이 필요하다.

### AI-01. 후보 경험 경량화

- [x] Solar 후보는 관련도 1위 한 개만 전달한다.
- [x] 후보 데이터에 경험 원문 transcript를 포함하지 않는다.
- [x] 관련도 1위 후보의 감정 맥락까지 검색 점수에 반영한다.
- [x] 서버에서도 매칭 후보 상한을 한 개로 제한한다.
- [x] 매칭 Solar 프롬프트에서 잘못 섞여 있던 어르신 답변 유효성 지침을 제거했다.
- [x] 고민 분석 20초·1200토큰, 매칭 25초·900토큰, 답변 정리 30초·1800토큰, 감사 메시지 15초·500토큰 설정을 확인했다.

### AI-02. 멘토 및 경험 ID 검증

- [x] Solar가 후보에 없는 경험 ID를 반환하면 거부한다.
- [x] 존재하지 않는 mentorId 또는 experienceId를 저장하지 않는다.
- [x] mentorId와 experienceId의 실제 소유 관계를 검증한다.
- [x] 선택 완료 후 실제 아카이브의 원문·편지·안전 정보를 복원한다.

### AI-03. Solar 실패

- [x] Solar 실패 시 규칙 기반 fallback 결과를 반환한다.
- [x] fallback 결과도 최종 경험 한 개만 유지한다.
- [x] 형식이 불완전한 Solar 응답을 정상 성공으로 처리하지 않는다.
- [x] 일시적인 Upstage 서버 오류는 한 번 재시도한다.

## 3. 로컬 데모 데이터베이스

사용 키:

```text
eobom_demo_users_v1
eobom_demo_messages_v1
eobom_demo_notifications_v1
```

### DB-01. 계정

- [x] `dummy_mentors.json`의 사용 가능한 멘토만 사전 등록한다.
- [x] 어르신 이름과 나이가 모두 일치할 때만 mentorId를 확인한다.
- [x] 같은 임산부 이름과 상태로 재로그인하면 같은 pregnantUserId를 사용한다.
- [x] 임의의 어르신 계정을 새 멘토로 만들지 않는다.

### DB-02. 질문 분리

- [x] 질문마다 고유한 questionId를 생성한다.
- [x] pregnantUserId 기준으로 임산부 질문을 분리한다.
- [x] mentorId 기준으로 어르신 질문을 분리한다.
- [x] 다른 멘토에게 배정된 질문을 조회하지 않는다.
- [x] 새로고침 후에도 질문이 유지된다.

### DB-03. 답변 및 알림

- [x] 배정된 멘토만 답변을 저장할 수 있다.
- [x] 같은 질문에 중복 답변을 저장하지 않는다.
- [x] 답변 완료 시 해당 임산부에게 읽지 않은 알림을 생성한다.
- [x] 답변을 확인한 임산부만 읽음 처리할 수 있다.
- [x] 감사 메시지는 배정된 멘토에게만 전달한다.
- [x] 다른 멘토에게 답변 기록과 감사 알림을 노출하지 않는다.

### DB-04. 감사 메시지 사실성

- [x] Solar 감사 요청에는 임산부가 직접 남긴 reaction만 전달한다.
- [x] 고민 요약과 멘토 경험 제목·선정 근거를 Solar 감사 요청에서 제외한다.
- [x] 감사 문장 첫 부분에 실제 reaction이 포함되어 있는지 검증한다.
- [x] “두려움이 해소되었다”, “극복했다”, “다행이다”처럼 확인되지 않은 변화를 차단한다.
- [x] 품질 검증에 실패하면 추가 Solar 호출 없이 reaction만 담은 문장으로 교체한다.
- [x] 배정 멘토 화면에는 교체된 안전한 감사 문장이 표시된다.
- [x] 기존 저장 데이터는 `feedbackReaction`을 우선 표시한다.
- [x] reaction이 없는 과거 기록의 해소·극복·다행 문장은 중립적인 감사 안내로 대체한다.

### DB-05. 자동 초기화 제거

- [x] 일회성 자동 DB 초기화 코드를 제거했다.
- [x] 이후 새로고침으로 사용자·질문·답변·알림이 삭제되지 않는다.
- [x] 로그아웃 시 영구 데모 DB 키를 삭제하지 않는다.

## 4. 어르신 답변 유효성 및 AI 편지

### MA-01. 관련 없는 답변

입력:

```text
응 근데 도착한 질문을 받을 방법은
```

- [x] OFF_TOPIC으로 판정한다.
- [x] 경험 카드와 편지를 생성하지 않는다.
- [x] 실제 경험을 다시 들려달라는 안내를 반환한다.

### MA-02. 너무 짧거나 불분명한 답변

입력:

```text
잘 모르겠어요
```

- [x] TOO_SHORT 또는 UNCLEAR로 판정한다.
- [x] 경험 카드와 편지를 생성하지 않는다.

### MA-03. 실제 경험

- [x] 출산·육아 실제 경험은 VALID로 처리한다.
- [x] experienceCard와 letter를 생성한다.
- [x] 선택된 경험 정보가 없어도 transcript와 질문으로 정리할 수 있다.

### MA-04. 편지 정리 품질

- [x] transcript를 녹취록처럼 그대로 복사하지 않도록 Solar 프롬프트를 보강했다.
- [x] 반복·추임새·군더더기·끊긴 말을 첫 응답 안에서 정리하도록 지시한다.
- [x] 상황 → 마음과 이유 → 행동 → 확인된 결과 순서로 재구성하도록 지시한다.
- [x] 세 가지 이상의 사실은 역할이 다른 2~3개 문단으로 구성하도록 지시한다.
- [x] 단순한 문장 부호·띄어쓰기·어미 변경을 정리 결과로 허용하지 않는다.
- [x] 원문에 없는 사건·인물·행동·성공 결과 추가를 금지한다.
- [x] 긴 원문과 편지가 90% 이상 유사하면 정리 완료로 처리하지 않는다.
- [x] 유사도 품질 미달 시 Solar 자동 추가 호출 없이 원문만 임시 저장한다.
- [x] 정상 편지 생성은 Solar 한 번의 호출로 완료한다.

### MA-05. AI 설명 문체

- [x] `emotional tone`을 `감정의 흐름`으로 정리한다.
- [x] `명확화`를 `명확하게 다듬었습니다`로 정리한다.
- [x] `포함되지 않음`을 `포함하지 않았습니다`로 정리한다.
- [x] 수정 내역과 안전 안내를 자연스러운 한국어 문장형으로 표시한다.

### MA-06. Solar 시간 초과

- [x] 원문만 임시 저장한다.
- [x] AI 정리 완료 상태로 처리하지 않는다.
- [x] Mock 또는 fallback 같은 개발 문구를 사용자에게 표시하지 않는다.
- [x] 다시 정리하기 동선을 제공한다.

## 5. STT 및 녹음

- [x] 최종 인식이 늦어도 화면의 중간 인식 문장을 transcript로 보존한다.
- [x] 녹음 시작 버튼 연속 클릭 시 두 번째 녹음 시작을 거부한다.
- [x] 녹음 답변과 audioUrl을 질문 ID에 연결한다.
- [x] 실제 Chrome 마이크 권한 허용 후 30초 이상 녹음 수동 확인
- [x] 실제 모바일 브라우저에서 녹음·재생 수동 확인

## 6. 화면 및 로그인 상태

### UI-01. 임산부

- [x] 분석 중 새로고침 시 저장된 고민으로 분석을 재실행한다.
- [x] 답변 도착 전에는 프로필 알림을 표시하지 않는다.
- [x] 답변 도착 후 프로필에만 알림을 표시한다.
- [x] 마이페이지에서 본인의 누적 질문과 답변만 표시한다.

### UI-02. 어르신

- [x] 질문이 없으면 답변 제출을 차단한다.
- [x] AI가 정리한 고민 요약과 어르신용 질문을 함께 표시한다.
- [x] 질문이 여러 개면 답변할 고민을 선택할 수 있다.
- [x] 질문 선택 전에는 녹음 영역을 숨긴다.
- [x] 다른 이야기 또 들려주기 후 남은 질문 목록을 다시 불러온다.
- [x] 어르신 프로필에는 질문 알림 숫자를 표시하지 않는다.
- [x] 경험 키워드를 해시태그 칩으로 표시한다.
- [x] 내용 수정하기·최종 전달하기 버튼을 AI 상세 설명보다 위에 표시한다.
- [x] 어르신 화면의 본문·버튼·태그·안내 폰트를 확대했다.

### UI-03. 로그인과 로그아웃

- [x] 로그인은 현재 브라우저 탭에서만 유지한다.
- [x] 같은 탭의 새로고침과 페이지 이동에서는 로그인 상태를 유지한다.
- [x] 새 탭 또는 새 브라우저 세션에서는 로그아웃 상태로 시작한다.
- [x] 보호 페이지 직접 접근 시 index.html로 이동한다.
- [x] 로그아웃 시 질문·답변·알림 영구 DB를 보존한다.

## 7. 누적 기록 및 저장 공간

- [x] 새 고민을 시작해도 과거 고민 기록을 누적한다.
- [x] 하나의 고민에 연결된 답변 기록을 보존한다.
- [x] 결과 확정 시 같은 답변이 중복 저장되지 않는다.
- [x] 현재 화면 초기화가 영구 기록을 삭제하지 않는다.
- [x] 기존 단일 세션 데이터를 처음 읽을 때 호환한다.
- [x] LocalStorage 공간 부족 시 과거 음성만 제외하고 텍스트 기록을 보존한다.

## 8. 영상 촬영 전 수동 체크리스트

- [x] 서버 실행 후 Solar 환경 변수 정상 로딩 확인
- [x] 임산부 로그인 → 고민 입력 → 추가 질문 처리 확인
- [x] 매칭된 멘토 이름·나이·태그 확인
- [x] 고민 전달 후 로그아웃 확인
- [x] 선택된 멘토의 정확한 이름·나이로 로그인 확인
- [x] AI 고민 요약과 어르신 질문이 모두 보이는지 확인
- [x] 질문 2개 이상 전송 후 선택 목록 확인
- [x] 실제 마이크 녹음 및 STT 확인
- [x] AI 편지가 원문 복사가 아닌 정리된 문단인지 확인
- [x] 내용 수정하기·최종 전달하기 버튼 위치 확인
- [x] 최종 전달 후 다른 이야기 또 들려주기 확인
- [x] 임산부 재로그인 후 프로필 답변 알림 확인
- [x] 마이페이지 편지·음성·안전 안내 확인
- [x] 감사 반응 전송 및 해당 멘토 기록 확인
- [x] 페이지 새로고침 후 질문·답변 데이터 유지 확인
- [x] 탭을 닫고 다시 열었을 때 로그아웃 상태 확인

## 9. 현재 판정

```text
자동 테스트: 통과 (43/43)
구문 검사: 통과
자동 DB 리셋 제거: 완료
실제 Solar 네트워크 호출: 완료
실제 마이크 및 모바일 레이아웃: 완료
```
