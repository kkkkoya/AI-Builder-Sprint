# 이어봄 AI 활용 증빙 문서

## 1. AI 활용 범위

이어봄은 AI가 사용자를 대신해 위로하거나 정답을 제시하는 서비스가 아닙니다. 임산부의 고민을 분석하고, 먼저 비슷한 시간을 살아본 어르신의 실제 경험을 찾아 연결하며, 어르신의 답변을 읽기 쉬운 경험 카드와 편지로 정리하는 데 AI를 활용합니다.

서비스 실행에 사용하는 AI와 프로젝트 개발에 활용한 AI 도구는 다음과 같이 구분됩니다.

### 1.1 서비스 실행 AI: Upstage Solar Pro 3

- 모델 ID: `solar-pro3`
- 사용 위치: Vercel 서버리스 함수 `api/solar.js`
- API 키: 서버 환경변수 `UPSTAGE_API_KEY`
- 주요 역할:
  - 임산부 고민 분석과 추가 질문 생성
  - 검색된 경험 후보의 의미 적합성 검토
  - 어르신에게 전달할 질문 생성
  - 어르신 STT 답변의 유효성 판단
  - 경험 카드, 편지, 수정 내역과 안전 안내 생성
  - 임산부의 반응을 감사 메시지로 정리

Solar API 키는 브라우저 코드에 포함하지 않으며 서버에서만 사용합니다.

### 1.2 음성 인식과 녹음

- STT: 브라우저 Web Speech API
- 언어: `ko-KR`
- 음성 저장: `MediaRecorder`

별도의 외부 STT 모델을 호출하지 않습니다. 브라우저에서 음성을 텍스트로 변환하면서 녹음 데이터도 함께 보존합니다.

### 1.3 개발 과정에서 활용한 AI 도구

다음 도구는 서비스 이용 중 사용자 데이터를 처리하는 런타임 모델이 아니라, 기획·개발·검토를 위한 보조 도구로 활용했습니다.

| 도구 | 활용 내용 |
| :--- | :--- |
| OpenAI Codex | 프로젝트 파일 분석, 코드 구현과 수정, 디버깅, 테스트 실행, Git 작업 보조 |
| GPT-5.6 | 요구사항 정리, 기능 흐름 검토, 오류 원인 분석, AI 응답 구조와 프롬프트 검토 |
| Google Gemini Pro | 기획 아이디어와 UI 흐름 검토, 문구·구성에 대한 교차 검토 |

Codex, GPT-5.6과 Gemini Pro는 배포된 이어봄 서비스의 API 호출 경로에는 포함되지 않습니다.

---

## 2. 서비스 AI 호출 구조

브라우저의 `api.js`가 `/api/solar`를 호출하고, 서버의 `api/solar.js`가 Upstage Solar API와 통신합니다.

| 처리 단계 | 공개 함수 | 서버 action |
| :--- | :--- | :--- |
| 고민 분석 | `analyzeConcern()` | `analyze-concern` |
| 경험 연결과 질문 생성 | `matchExperience()` | `match-experience` |
| 어르신 답변 정리 | `processMentorAnswer()` | `process-mentor-answer` |
| 감사 메시지 생성 | `createImpactFeedback()` | `create-impact-feedback` |

감사 메시지 요청에는 임산부가 직접 선택하거나 작성한 `reaction`만 전달합니다. 고민 요약과 멘토 경험 내용은 보내지 않습니다. 생성 결과에 실제 reaction이 없거나 해소·해결·극복·다행처럼 확인되지 않은 변화가 포함되면 추가 API 호출 없이 reaction만 담은 안전한 문장으로 교체합니다.

모든 공개 함수는 `ok`, `data`, `error`, `meta`를 포함하는 공통 응답 구조를 유지합니다. Solar 요청이 실패하면 기능별 규칙 기반 처리 또는 임시 저장 흐름으로 전환해 화면이 무한 로딩 상태에 남지 않도록 했습니다.

---

## 3. 고민 분석과 경험 연결

### 3.1 고민 분석

Solar는 사용자의 전체 문장과 추가 답변 이력을 함께 읽고 다음 내용을 구조화합니다.

- 서비스 범위와 추가 질문 필요 여부
- 복합 고민과 우선순위
- 감정과 문장 근거
- 현재 상황과 필요한 경험
- 표준 경험 태그
- 안전 안내 우선 여부

감정만 있고 원인이나 실제 상황을 알 수 없는 고민은 임의로 매칭하지 않고 한 번에 하나의 추가 질문을 생성합니다. 출산·육아·가족·경력처럼 주제만 확인되는 경우에는 해당 주제에서 가장 걱정되는 실제 상황을 묻습니다. 선택지를 길게 나열하지 않으며, 추가 답변은 원문과 함께 다시 분석해 구체적인 상황이 확인되면 경험 검색으로 진행합니다.

Solar의 `needsClarification`과 `route`가 일시적으로 다르게 반환되더라도 JavaScript 정규화 단계에서 `CLARIFICATION`으로 통일합니다. 화면에서도 두 신호를 모두 확인해 추가 질문 영역이 누락되지 않도록 처리합니다.

### 3.2 저지연 경험 매칭

현재 더미 아카이브에는 멘토 6명과 경험 카드 12개가 등록되어 있습니다. 매칭 과정은 다음과 같습니다.

```text
전체 경험 아카이브
→ matching.js가 태그·감정·필요·문장 맥락으로 관련도를 계산
→ 관련도 1위 후보 한 개만 경량화
→ Solar가 의미 적합성, 근거와 한계를 검토
→ 어르신용 질문 생성
→ JavaScript가 mentorId와 experienceId를 실제 데이터와 검증
→ 검증된 원본 경험 데이터 복원
```

응답 시간을 줄이기 위해 Solar에는 후보 한 개의 제목, 요약, 태그, 감정, 도움 유형과 안전 정보만 전달합니다. 경험 원문 `transcript`와 기존 편지 `letter`는 매칭 요청에 포함하지 않습니다.

Solar가 반환한 ID가 실제 아카이브에 없거나 전달한 후보와 일치하지 않으면 결과를 거부합니다. Solar 요청 실패 시에는 동일한 검색 결과를 이용한 규칙 기반 fallback을 사용합니다.

---

## 4. 어르신 답변 처리와 안전성

`processMentorAnswer()`는 어르신 답변을 다음 상태로 먼저 판정합니다.

- `VALID`: 질문과 관련된 실제 경험
- `TOO_SHORT`: 경험을 확인하기에 너무 짧은 답변
- `OFF_TOPIC`: 서비스 사용법 등 질문과 무관한 답변
- `UNCLEAR`: 실제 경험인지 판단하기 어려운 답변

`VALID`인 경우에만 경험 카드와 편지를 생성합니다. 편지는 다음 원칙으로 정리합니다.

- STT 원문에 있는 사실만 사용
- 어르신의 1인칭 시점과 말투 유지
- 반복, 군더더기와 명백한 STT 오류 정리
- 상황, 당시의 마음, 실제 행동과 결과 순서로 재구성
- 원문에 없는 조언, 사건, 성공 결과 추가 금지
- 의료적·법률적 단정 금지

편지 생성은 첫 Solar 호출 안에서 완료합니다. 프롬프트는 녹취 문장 순서를 그대로 옮기지 않고, 반복·추임새를 덜어낸 뒤 상황·마음·행동·확인된 결과의 흐름으로 재구성하도록 요구합니다. 세 가지 이상의 사실은 2~3개 문단으로 나누며 단순한 문장 부호·띄어쓰기·어미 변경은 허용하지 않습니다.

응답 속도를 늦추지 않기 위해 원문 유사도 검사를 통과하지 못해도 Solar를 자동으로 다시 호출하지 않습니다. 긴 편지가 원문과 90% 이상 유사하면 AI 정리 완료로 표시하지 않고 원문만 임시 저장한 뒤 사용자가 기존 ‘다시 정리하기’를 선택할 수 있게 합니다. 정상 결과는 한 번의 API 호출로 끝납니다.

Solar 시간 초과 시에는 원문만 임시 저장하고 AI 정리 완료로 처리하지 않습니다. 사용자에게 다시 정리할 수 있는 동선을 제공하며 Mock이나 fallback 같은 개발 용어는 화면에 노출하지 않습니다.

---

## 5. API 제한 시간과 출력 설정

| 기능 | 프롬프트 생성 함수 | 최대 출력 토큰 | 제한 시간 |
| :--- | :--- | ---: | ---: |
| 고민 분석 | `buildAnalyzeConcernMessages()` | 1,200 | 20초 |
| 경험 연결 | `buildMatchExperienceMessages()` | 900 | 25초 |
| 어르신 답변 정리 | `buildProcessMentorAnswerMessages()` | 1,800 | 30초 |
| 감사 메시지 | `buildCreateImpactFeedbackMessages()` | 500 | 15초 |

복잡한 구조화 결과는 JSON 응답 모드를 사용합니다. 빠른 첫 요청에는 불필요한 `reasoning_effort` 선택 매개변수를 보내지 않으며, Upstage가 선택 매개변수를 거부하면 기본 요청 형식으로 다시 시도합니다.

---

## 6. 데모 데이터 연결 구조

해커톤 시연에서는 실제 서버 데이터베이스 대신 같은 브라우저의 LocalStorage를 사용합니다.

| 저장 키 | 저장 내용 |
| :--- | :--- |
| `eobom_demo_users_v1` | 임산부 계정과 사전 등록 멘토 계정 |
| `eobom_demo_messages_v1` | 질문, 배정 멘토, 답변, 음성, AI 편지와 감사 메시지 |
| `eobom_demo_notifications_v1` | 멘토 질문, 임산부 답변과 감사 메시지의 읽음 상태 |
| `eobom_session_v1` | 현재 화면 처리 상태와 호환용 세션 |

질문과 답변은 `questionId`, `pregnantUserId`, `mentorId`로 연결합니다. 로그아웃과 새로고침 이후에도 질문과 답변은 유지되며, 로그인 상태는 브라우저 탭을 닫으면 종료됩니다.

LocalStorage는 서로 다른 기기나 브라우저 사이에서 공유되지 않습니다. 실제 서비스에서는 인증과 서버 데이터베이스로 교체해야 합니다.

---

## 7. 테스트와 검증

현재 자동 테스트는 다음 영역을 확인합니다.
자세한 사항은 TEST_CASES.md

- Solar 전달 후보 한 개와 민감 원문 제외
- 존재하지 않는 멘토·경험 ID 거부
- fallback 결과와 원본 경험 복원
- 멘토별 질문·답변·알림 분리
- 중복 답변 방지와 누적 기록 보존
- 어르신 답변 유효성 판정
- 첫 호출 내 편지 구조화와 원문 복사 결과 차단
- 품질 보강용 자동 추가 호출 제거
- Solar 시간 초과 시 임시 저장
- STT 중간 결과 보존과 중복 녹음 방지
- 로그아웃·새로고침·브라우저 탭 로그인 상태
- 임산부·어르신 마이페이지 기록

검증 명령은 다음과 같습니다.

```bash
node --test tests/*.test.mjs
```

현재 자동 테스트 결과는 총 52개 통과, 실패 0개입니다. 여기에는 추가 질문 흐름, 편지 원문 복사 차단, 감사 요청의 reaction-only 입력, 확인되지 않은 회복 표현 차단과 기존 저장 기록 표시 검증이 포함됩니다. 실제 Solar 네트워크 통신은 배포 환경의 `UPSTAGE_API_KEY`와 `/api/solar` 응답을 브라우저 Network 탭에서 별도로 확인합니다.
